# Base URL backend API.
# Salin file ini menjadi ".env" lalu sesuaikan nilainya dengan environment kamu
# (lokal/staging/production). Selama backend belum tersedia, nilai ini belum
# dipakai secara aktif (lihat src/services/authService.js).
VITE_API_BASE_URL=http://localhost:3000/api
